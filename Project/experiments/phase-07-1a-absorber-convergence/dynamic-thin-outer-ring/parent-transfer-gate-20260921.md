# C8 dynamic thin-outer-ring — parent-transfer gate

## Gate result

**SUPERSEDED BY HUMAN INDEPENDENT-C8 AUTHORITY — 2026-09-21.** C8 is now an
independent Server-3 family built from scratch. It must not use, inspect,
transfer, or wait on a C7 artifact. Its replacement parent gate is: fresh
Server-3 all-wall build → C8-D0 all-wall development → selected C8-D0
case/data identity and trigger receipt → fresh C8 pressure child.

The C8 parent is required to be the exact selected Server-3 C8-D0
developed-field checkpoint, with every bottom band still a wall. It may not be
substituted with any historical prepared thin-outer pressure-outlet pair.

## Superseded Server-3-only attempt — 2026-09-21

This attempt is preserved as implementation evidence only and is not a C8
parent. The Server-3 live session was first found on a superseded Coupled
solver path. The C8 operator preserved that state to a Server-3 local recovery
pair, then reconstructed the 237k all-wall parent from the verified baseline
snapshot. The local build completed its save/reopen audit with Mixture/RNG, all five
bottom bands as stationary no-slip walls, the two expected fluid zones, and a
phase-2-only lower source density of `-364.0035739194401 kg/m³/s` over
`0.3212056374640879 m³`, integrating to exactly `-116.92 kg/s`.

The local parent was developed through active 100 under the C7-R25 synchronized
two-inlet/absorber schedule, then stopped by the project-contract correction.
It is not a C8 case and the ring was never opened. Its active-50 monitor smoke
passed; its local checkpoint root is
`C:\\Users\\syok443\\Documents\\FluentRuns\\Phase07\\C8\\c7-equivalent-checkpoints`.
Do not resume it. Preserve its artifacts, but begin Stage 1 only after the
selected Server-1 C7 transfer gate passes.

## Reconciled C7 status

The C7 execution task, [Phase 7.1A C7 — Server 1 inlet-development](codex://threads/01a0c16e-089f-71c1-98ca-fed952768e0d), is active. Its corrected `C7-R0-retry5`
manifest reports `RUNNING`; its active-50 monitor smoke passed and read back a
phase-2-only lower-absorber command of `-116.92 kg/s` (source density
`-364.00357391944044 kg/m³/s` over `0.3212056374640876 m³`). The five bottom
bands remain walls. It identifies the newly created Server 1 OneDrive all-wall
parent:

```text
C:\Users\syok443\OneDrive - The University of Auckland\P4P-Fluent-Artifacts\Phase07\C7\20260921T000000Z\P71A-thin-outer-baseline-prepared.cas.h5
C:\Users\syok443\OneDrive - The University of Auckland\P4P-Fluent-Artifacts\Phase07\C7\20260921T000000Z\P71A-thin-outer-baseline-prepared.dat.h5
```

This is an active C7 construction/execution parent, not yet the selected C7
developed-field checkpoint. The visible C7-R0 work is scheduled for 5,000
active iterations with the required checkpoint paths; the passed active-50
smoke is an instrumentation gate, not C7 development evidence. Therefore
there is not yet a selectable C7 pair to:

1. identify by exact case/data paths and SHA-256 values;
2. transfer to Server 3; or
3. reopen and read back on Server 3 before applying the ring mutation.

## Direct artifact and sync inspection — 2026-09-21

The C7 route is present and advancing; it must not be called absent merely
because the selection receipt is pending. Direct inspection found the exact
all-wall Server-1 parent case/data pair above in the macOS OneDrive mirror,
plus C7-R0 prepared/active-0 pairs for attempted and active restarts. At the
inspection time the active `C7-R0-retry5` manifest was `RUNNING` through active
`500` of `5,000`, with the prescribed all-wall controls.

The macOS mirror reports these large Fluent artifacts as `dataless` cloud
placeholders. That establishes OneDrive namespace/index visibility, but does
not claim a locally hydrated-byte hash or Server-3 reopen. The Server-1 runner
records its current local-disk checkpoints under:

```text
C:\Users\syok443\Documents\FluentRuns\Phase07\C7\C7-R0\20260921T014000Z\
```

and its only intended terminal publication path is:

```text
C:\Users\syok443\OneDrive - The University of Auckland\P4P-Fluent-Artifacts\Phase07\C7\finals\C7-R0\20260921T014000Z\C7-R0-active5000.cas.h5
```

This C8 operator's local attach profile has no Server-3 Fluent connection
credentials, so a direct Server-3 `file-exists`/reopen probe was not available
from this host at this time. That is a narrow transport-observability gap, not
evidence that Server 3 lacks the OneDrive files and not a reason to rebuild
while the C7 route is demonstrably live. The mandatory Server-3 pair-presence,
SHA-256, and reopen readback remain a pre-mutation C8 gate.

The historical `ThinOuterBaseline` family remains disqualified: its manifest,
[`P71A-thin-outer-ramp-baseline-server1-manifest.json`](../../../../PyAnsys/output/phase07_thin_outer_baseline/P71A-thin-outer-ramp-baseline-server1-manifest.json),
records `bottom-bottom-band1-thin-outer-separator-purnanto` as a pressure
outlet at `1,120,000 Pa`. It is the explicitly prohibited pre-opened ring
state, not a C7 all-wall parent.

## C8 run state

| Requirement | Status |
| --- | --- |
| New all-wall C7 build parent on Server 1 | Present; C7-R0 active |
| Exact C7 selected developed-field case/data pair | Pending C7 selection |
| C7 case/data SHA-256 identity receipt | Pending selected checkpoint |
| C7 lower-liquid development evidence for threshold selection | Pending C7 history |
| Transfer of the selected pair to Server 3 | Not started; selection is pending |
| Server 3 reopen/readback | Not started; selected pair is pending |
| C8 pressure ladder | Not started |
| C8 activation-timing variants | Not eligible |

Consequently no monitor-based lower-liquid threshold or persistence window can
be chosen honestly from C7 evidence, and no C8 pressure point is a valid
experiment yet. This is an execution dependency, not a scientific conclusion
about the ring intervention.

## Exact recovery input

When C7 completes and selects a development state, C8 will obtain the selected
checkpoint's paired `.cas.h5` and `.dat.h5` files, Server 1 SHA-256 values,
all-wall topology/readback receipt, and the base-flow lower-liquid history
identifying the trigger metric. C8 can then hash-match the transferred pair,
reopen it on Server 3, audit the five wall bands and baseline invariants,
declare a persisted lower-liquid trigger, and start the pressure ladder at
1.120 MPa.

## Claim limit

No C8 routing, vapor-loss, pressure-response, balance, or numerical claim is
made. The planned pressure values and stopping criteria remain valid only
after the required C7 parent gate is passed.

The executable handoff requirements and all child controls are fixed in the
[C8 deferred setup contract](deffered.md); only the C7-evidence-derived trigger numbers
and exact selected parent identity remain to be populated.
