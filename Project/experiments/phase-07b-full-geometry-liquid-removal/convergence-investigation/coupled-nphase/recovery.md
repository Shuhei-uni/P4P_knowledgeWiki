# E6 persistence recovery — requires restored API access

Confirmed: the prepared case reconstructed mesh and phase domains, then the prepared data read crashed during parallel result loading. All ranks reported SIGSEGV/termination. Live Boolean-only treatment and eight residual settings had passed, but no physical-field parity or scientific solve followed. See [results](results.md).

Hypothesis: enabling the primary phase equation after Hybrid initialization may leave equation storage that does not serialize/reload correctly at untouched N0. This is not proved; a Fluent defect, case-specific storage issue or other process fault remains possible. Focused official-documentation searches did not establish a known matching defect or guaranteed repair.

After Andy's API session is restored, reconcile its identity and idle state, inspect preserved E5 and clean N0 inputs via Fluent API, and retain all failed evidence. Never blindly reload the failed prepared data pair or relaunch the old job ID.

The smallest candidate recovery is enabling the supported N-phase option before SIMPLE Hybrid initialization so its equation storage is initialized, retaining the exact original Hybrid options, then applying unchanged Coupled/Off CFL20. Before saving/reopening a unique candidate pair, compare all original N0 physical/geometry arrays to the original reference (apart from the explicitly uncalibrated raw imbalance storage). Require exact parity; if it changes physical fields, this is not an equivalent repair and must be redesigned prospectively. Save/reopen and recheck every scientific setting, source, all eight residual monitors and physical arrays before any N50 solve. No extra iteration to allocate storage is preauthorized as an invisible preparation step.

This is a testable recovery design, not a verified fix or selected new scientific contrast. The scientific delta remains N-phase-only, tau0.02/CFL20, same full-feed physical state, N5000 absolute cap. API restoration is the current external prerequisite; no other remote execution route or session is authorized.

## Verified initialization representation repair

The retry2 controller stopped before solve because the immediate post-Hybrid expression counter was1, whereas its new pre-save guard required0. No solve command was issued. At that point cell ordering differed: a unique rounded-centroid bijection (1e-10m keys) gave exactly equal U/V/W/P/K/epsilon/alpha values; actual coordinate discrepancies were below9e-16m and volume discrepancies below1.7e-19m3. These are representation differences, not an accepted changed initial physical field.

A separate source-free SIMPLE+Nphase checkpoint then saved and reopened successfully. Reload returned the native counter to0 and restored **exact original ordered geometry and physical arrays**, with zero solve iterations. Machine evidence lives in `e6/retry2/post-reopen-arrays/` and `e6/retry2/persistence-probe-2/`. The original probe receipt is retained: its post-read expected-counter1 assertion failed, not the data read.

For the next unique retry, the pre-initial-save check may record counter0 or1, with zero solve issued. This one pre-save check may use a proved bijection, requiring actual centroid distance<=1e-14m, cell-volume absolute difference<=1e-18m3 and exact mapped physical values. All prepared/post-reload checks retain counter0 and exact original physical/geometry equality. No residual reset, counter setter, index subtraction or hidden warm-up is used. The Coupled/source prepared-pair reopen still requires independent proof before N50; successful source-free persistence alone does not establish it.

## N50 diagnostic semantics and unchanged-state continuation

Retry4 completed50 solver iterations before the whole-cell inventory parity assertion stopped its controller. Its unchanged liveN50 endpoint was preserved in a unique paired checkpoint. All eight residuals and exact-face flux rows1–50 are present; speed rows1–49 preceded the failed scheduledN50 snapshot. No numerical fatal marker was reported.

At idleN50, raw secondary SV_VOF integration was0.0037925705703864567m³, while ordinary `Volumefraction`, `Normalizedvolumefraction` and native `phase-2-vof` volume reports all returned0.003794473226225399m³. The raw primary+secondary sum ranged0.9913589702242562–1.0015089692579497. Cellwise `raw_alpha2/(raw_alpha1+raw_alpha2)` integrated to0.003794473224950102m³, matching native within the existing snapshot tolerance(rtol1e-9,atol1e-12). Geometry was exactly unchanged. Both ordinary and normalized primary native integrals also agreed. This is an observed reporting relationship at this state, not an undocumented universal SV storage claim.

Fluent2025R2 [UG27.8.1.3.2](https://ansyshelp.ansys.com/public/Views/Secured/corp/v252/en/flu_ug/flu_ug_sec_multiphase_solution.html) documents solving all phase equations then scaling fractions to sum to1; the [expression appendix](https://ansyshelp.ansys.com/public/Views/Secured/corp/v252/en/flu_ug/flu_ug_expressions_Appendix_fieldvars.html) lists ordinary and normalized fields separately without establishing SV_VOF storage semantics. The local native comparison establishes the repair basis.

E6 snapshots now retain **both raw phase arrays**, raw inventory and phase-sum extrema. Their separate normalized reconstruction must match native inventory at every snapshot using the same original tolerance. Raw arrays are never replaced. Existing speed/k/epsilon and iteration checks remain. The independent audit uses the same E6 inventory tolerance; historical cases retain their prior check. This repairs instrumentation only; no solver/source definition is changed. A second snapshot atN55 explicitly tests persistence of the reporting relationship.

Recover the missingN50 snapshot from the preserved unchanged live state, verify the complete prefixes, then resume that same state with no reload/reinitialization to55(recovery smoke),500 and every500 through the original absolute5000 cap. Preserve the failed run and receipt; the new resume manifest links original histories and records the recovery. Default scheduled/event snapshots remain required, with the additionalN55 semantic check. Source-inclusive budgets continue using actual native applied source once; improved normalized reports do not erase raw phase-sum defects or establish conservation.
