{
  "failure_modes": [
    {
      "symptom": "Path exists in notes but not in live PyFluent object",
      "likely_causes": ["wrong Fluent version", "parent model inactive", "different solver mode", "object not reacquired after activation"],
      "response": ["list child_names/command_names", "refresh/reacquire parent", "check GUI/TUI", "log path as version-dependent"]
    },
    {
      "symptom": "Setter accepts call but readback did not change",
      "likely_causes": ["invalid option silently ignored", "wrong object instance", "setting overwritten by later parent change"],
      "response": ["always read back", "move setting later in order", "try TUI fallback", "log manual fix"]
    },
    {
      "symptom": "DPM injection surface binding fails with string/symbol error",
      "likely_causes": ["surface setter expects symbol/list format", "injection type not surface yet", "surface name differs from boundary zone id", "PyFluent wrapper bug"],
      "response": ["create default injection first", "set injection type=surface", "reacquire", "inspect allowed values", "try TUI injection edit", "manual GUI fallback"]
    },
    {
      "symptom": "Only anthracite appears for inert particles",
      "likely_causes": ["inert-particle material category not activated", "material created in wrong category", "particle type not set/reacquired", "Fluent default only before injection object exists"],
      "response": ["enable DPM", "create default injection", "set particle type=inert", "reacquire", "create/copy water-like inert material", "assign material", "read back"]
    },
    {
      "symptom": "Surface tension inactive",
      "likely_causes": ["using Mixture instead of VOF", "phases not assigned", "phase pair missing", "surface tension not available for this model combination"],
      "response": ["verify multiphase model", "assign phases", "inspect phase interaction tree", "do not force VOF paths under Mixture"]
    },
    {
      "symptom": "Boundary condition fields disappear/reset",
      "likely_causes": ["boundary type changed after setting values", "model enabled after BC setup", "phase count/material changed"],
      "response": ["set boundary type first", "enable all relevant models before BCs", "set BC values late", "read back after phase/model changes"]
    },
    {
      "symptom": "DPM update or per-injection report fails but an older report is still parseable",
      "likely_causes": ["DPM update command failed", "prior scratch file was not removed", "per-injection summaries were enabled after tracking", "report wrapper returned without new output"],
      "response": ["treat DPM update failure as fatal", "delete the prior scratch report", "require a newly readable report for every injection", "reconcile fate mass against current injected mass"]
    },
    {
      "symptom": "Fluent GUI injection panel raises ASSQ improper list",
      "likely_causes": ["saved injection list contains a wrapper-generated structure the GUI cannot render", "surface binding is stale after reporting", "version-specific Scheme/list serialization issue"],
      "response": ["reload the matching case/data pair", "inspect injection state through PyFluent", "reapply each injection sequentially and read back", "save a new post-DPM case/data pair", "classify as requires manual GUI cleanup if the error persists"]
    },
    {
      "symptom": "Residual plot shows fewer points than the requested iteration budget",
      "likely_causes": ["n_display limit", "n_save limit", "history compaction", "data file not loaded with the case"],
      "response": ["load the matching data file", "inspect residual n_save and n_display", "check exported monitor x-axis evidence", "do not infer completed iterations from plot length alone"]
    }
  ]
}
