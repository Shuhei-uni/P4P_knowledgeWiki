# Validation Targets Index

## Purpose
Store project-facing notes for target sets, anchor selection, and machine-readable target locations.

## Use Rule
- Keep the human-readable explanation here.
- Keep machine-readable YAML, JSON, or generated target tables in `PyAnsys`.
- Link the exact file path used for automation so claim gating is traceable.

## Planned Target Sets
- `07-baseline-targets`:
  - intended for setup `07` baseline acceptance, trend checks, and future partner/field comparison data.

## External Links To Maintain
- machine-readable target manifest path:
  - `../../../PyAnsys/knowledge/vnv_targets/`
- claim-rule path:
  - `../../../PyAnsys/knowledge/claim_rules/`
