interface:
  display_name: "Direct Fluent Use"
  short_description: "Start and close pinned Fluent 2025 R2."
  default_prompt: "Use $direct-fluent-use only to start or close the pinned local Fluent session."
policy:
  allow_implicit_invocation: false
