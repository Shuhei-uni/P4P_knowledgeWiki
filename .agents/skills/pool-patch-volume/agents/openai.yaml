interface:
  display_name: "Pool Patch Volume"
  short_description: "Apply an authorized pool patch through MCP and verify volume."
  default_prompt: "Use $pool-patch-volume to apply only the authorized pool initial condition through MCP, verify selected-cell and actual liquid volumes, and report any separately requested pressure evidence."
