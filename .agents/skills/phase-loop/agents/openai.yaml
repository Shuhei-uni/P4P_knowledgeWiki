interface:
  display_name: "Phase Loop"
  short_description: "Advance the CFD phase autonomously."
  default_prompt: "Use $phase-loop to advance the active phase to the strongest evidence-backed conclusion inside its recorded scope. Recover technical blockers autonomously."

policy:
  allow_implicit_invocation: true
